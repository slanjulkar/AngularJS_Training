
function authenticate(emailAddress, password)
{
    let emailAddress = document.getElementById("txtemail").value;
    let password = document.getElementById("txtpass").value;

    
    Validate(emailAddress,password)
}